apiVersion: batch/v1
kind: Job
metadata:
  name: test-hook-argocd
  namespace: kube-system
  annotations:
    argocd.argoproj.io/hook: Sync
    argocd.argoproj.io/sync-wave: "2"
    argocd.argoproj.io/hook-delete-policy: HookSucceeded
spec:
  template:
    spec:
      containers:
      - image: registry.redhat.io/openshift4/ose-cli:v4.9
        command:
          - /bin/bash
          - -c
          - |
            while ! oc get InstallPlan -n openshift-operators-redhat|grep "elasticsearch-operator.5.4.2   Manual     true" ; do
              echo "Retry patch InstallPlane elasticsearch"
              oc patch installplan $(oc get InstallPlan -n openshift-operators-redhat|grep "elasticsearch-operator.5.4.2   Manual"|awk '{print $1}') --namespace openshift-operators-redhat --type merge --patch '{"spec":{"approved":true}}'
              sleep 2
            done


        imagePullPolicy: Always
        name: test-hook-argocd
      serviceAccount: argocd-manager
      serviceAccountName: argocd-manager
      dnsPolicy: ClusterFirst
      restartPolicy: OnFailure
      terminationGracePeriodSeconds: 30
