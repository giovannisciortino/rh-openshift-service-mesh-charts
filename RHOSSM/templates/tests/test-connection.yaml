apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "RHOSSM.fullname" . }}-test-connection"
  labels:
    {{- include "RHOSSM.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "RHOSSM.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
